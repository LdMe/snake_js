
// Dibujar tabla
function drawTable() {
    for (var i = 0; i < cellsX; i++) {
        for (var j = 0; j < cellsY; j++) {
            drawCell(i, j);
        }
    }
}


// Dibujar tabla
function drawTable() {
    /*TODO: para i de 0 a cellsX*/ {
        /*TODO: para j de 0 a cellsY*/{
            drawCell(i, j);
        }
    }
}
